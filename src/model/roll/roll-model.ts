export interface RollData {
    label: string;
    roll: number;
    modifier: number;
    total: number;
    playerId: string;
    playerName: string;
    timestamp: number;
}

export const nameMessage = "roll";